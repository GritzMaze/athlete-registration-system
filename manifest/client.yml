apiVersion: apps/v1
kind: Deployment
metadata:
  name: athelete-registration-system-client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: athelete-registration-system-client
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: athelete-registration-system-client
    spec:
      containers:
        - name: client
          image: $CLIENT_IMAGE
          ports:
            - name: http
              containerPort: 8080
          resources:
            requests:
              cpu: 200m
              memory: 100Mi
            limits:
              cpu: 300m
              memory: 200Mi